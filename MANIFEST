# TODO
# - could I adapt clone() to do a better dump()?

rocks.lua

lx.lua
fx.lua
sx.lua
tx.lua

list.lua
map.lua
set.lua

test_sx.lua
test_tx.lua

test_list.lua
test_map.lua
test_set.lua
check.lua

st.sh

# to import the modules use this approach:
#   package.path = package.path .. ";" .. os.getenv("HOME") .. "/app/lua/?.lua"
#   local sx = require("lx.sx") -- etc

# to import the modules in each other and and by importers the modules
# must use this approach:
#   local ok, tx = pcall(require, "lx.tx")
#   if not ok then tx = require("tx") end

# /home/mark/opt/luarocks/share/lua/5.4/pl/

# vim: syn=yaml
